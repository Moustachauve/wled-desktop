import { DeviceThemeDirective } from './device-theme.directive';

describe('DeviceThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceThemeDirective();
    expect(directive).toBeTruthy();
  });
});
